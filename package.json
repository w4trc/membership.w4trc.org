{
  "name": "karc-membership",
  "version": "1.0.0",
  "description": "Kingsport Amateur Radio Club - Membership Management System",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute karc-membership --remote --file=schema/001_initial.sql",
    "db:seed": "wrangler d1 execute karc-membership --remote --file=schema/002_seed.sql",
    "db:init:local": "wrangler d1 execute karc-membership --local --file=schema/001_initial.sql",
    "db:seed:local": "wrangler d1 execute karc-membership --local --file=schema/002_seed.sql",
    "setup-admin": "node scripts/setup-admin.js"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^3.0.0",
    "bcryptjs": "^2.4.3"
  }
}
